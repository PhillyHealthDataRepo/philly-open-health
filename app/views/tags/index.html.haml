%h1 Listing tags

%table
  %tr
    %th Name
    %th
    %th
    %th

  - @tags.each do |tag|
    %tr
      %td= tag.name
      %td= link_to 'Show', tag
      %td= link_to 'Edit', edit_tag_path(tag), class: "btn btn-default"
      %td= link_to 'Destroy', tag, :method => :delete, :data => { :confirm => 'Are you sure?' }, class: "btn btn-danger"


= link_to 'New Tag', new_tag_path, class: "btn btn-info"
